spring:
  application:
    name: mylibrary-config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://infygit.ad.infosys.com/mylibrary/mylibrary-config-service.git
          username: ${CONFIG_REPO_USER}
          password: ${CONFIG_REPO_PWD}

server:
  port: 8888